<%= semantic_form_for @tip do |f| %>
  <%= f.text_area :content, :class => 'ckeditor' %>

  <div class="form-actions">
    <%= f.submit 'Save Tip', class: 'btn btn-primary' %>
    <%= link_to 'Cancel', :back, class: 'btn btn-link' %>
  </div>
<% end %>
