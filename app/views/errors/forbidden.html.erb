<% title "Forbidden (403)" %>

<div class="profile-page col-md-5 col-md-offset-3 text-center">
  <h1>Forbidden</h1>
  <p class="lead">You are not authorized to perform that action.</p>

  <p>
    Use the links above or jump straight
    <% if user_signed_in? %>
      <%= link_to "back to your profile", main_app.user_path(current_user) %>.
    <% else %>
      <%= link_to "back to our homepage", main_app.root_path %>.
    <% end %>
  </p>

  <% unless user_signed_in? %>
    <hr />
    <p>
      Don't have an account? <%= link_to 'Sign up today', main_app.new_user_path %> or <%= link_to 'log in', czardom_models_engine.login_path %>.
    </p>
  <% end %>
</div>

