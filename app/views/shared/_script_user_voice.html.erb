  <script>
    // Include the UserVoice JavaScript SDK (only needed once on a page)
    UserVoice=window.UserVoice||[];(function(){var uv=document.createElement('script');uv.type='text/javascript';uv.async=true;uv.src='//widget.uservoice.com/8Kp0tKo9oX6oOV7tnPO96g.js';var s=document.getElementsByTagName('script')[0];s.parentNode.insertBefore(uv,s)})();

    //
    // UserVoice Javascript SDK developer documentation:
    // https://www.uservoice.com/o/javascript-sdk
    //

    // Set colors
    UserVoice.push(['set', {
      accent_color: '#806F68',
      trigger_color: 'white',
      trigger_background_color: '#806F68'
    }]);

    // Identify the user and pass traits
    // To enable, replace sample data with actual user traits and uncomment the line
    UserVoice.push(['identify', {
      email: '<%= current_user.email %>',
      name: '<%= current_user.full_name %>',
      id: '<%= current_user.id %>',
      created_at: <%= current_user.created_at.to_i %>,
      account: {
        id: '<%= main_app.user_url(current_user) %>'
      }
    }]);

    // Add default trigger to the bottom-right corner of the window:
    UserVoice.push(['addTrigger', { mode: 'satisfaction', trigger_position: 'bottom-right' }]);

    // Or, use your own custom trigger:
    //UserVoice.push(['addTrigger', '#id', { mode: 'satisfaction' }]);

    // Autoprompt for Satisfaction and SmartVote (only displayed under certain conditions)
    UserVoice.push(['autoprompt', {}]);

  
   !function(g,s,q,r,d){r=g[r]=g[r]||function(){(r.q=r.q||[]).push(
  arguments)};d=s.createElement(q);q=s.getElementsByTagName(q)[0];
  d.src='//d1l6p2sc9645hc.cloudfront.net/tracker.js';q.parentNode.
  insertBefore(d,q)}(window,document,'script','_gs');

  _gs('GSN-527997-J');
</script>
  <% if user_signed_in? %>
 <script>
  var userID = '<%= current_user.id %>';

  var properties = {
    id: userID,
    first_name: '<%= current_user.first_name %>',
    last_name: '<%= current_user.last_name %>',
    email: '<%= current_user.email %>',
    username: '<%= current_user.slug %>',
    avatar: '<%= current_user.image_url(:small) %>',
    custom: {
      profile_url: '<%= main_app.user_url(current_user) %>'
    }
  };

  _gs('identify', userID, properties);
  
  // <!--   $('.richedit').redactor({-->
  // <!--     buttons: ["bold", "italic", "link"],-->

  // <!--    initCallback: function() {-->
  // <!--      $(this.$editor).atwho({-->
  // <!--        at: "@",-->
  // <!--        displayTpl: "<li data-value='${name}'>${name}</li>",-->
  // <!--        limit: 5,-->
  // <!--       insertTpl: "<a href='${slug}' rel='mention'>${name}</a>",-->
  // <!--         data: <%#= current_user.following.includes(:following).map { |f| { slug: f.following.slug_url, name: f.following.full_name } }.to_json.html_safe %>
  // <!--     });-->

  // <!--      $('.redactor-editor').on('click', 'a', function(e) {-->
  // <!--        e.preventDefault();-->
  // <!--      });-->
  // <!--    }-->
  // <!--});-->
  
  </script>
  <% end %>