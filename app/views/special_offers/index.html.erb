<div class='col-md-9'>
  <h1><%= 'No Special Offer for now' if @special_offers.blank?-%></h1>
  <% @special_offers.each do |special_offer| %>
    <div class="col-md-12">
      <p><%= special_offer.content.html_safe %></p>
    </div>
  <% end %>
</div>